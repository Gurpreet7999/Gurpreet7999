name: Generate Snake

on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest

    steps:
      - name: Generate snake
        uses: Platane/snk@v3
        with:
          github_user_name: Gurpreet7999
          outputs: dist/snake.svg

      - name: Push snake
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: output
          folder: dist
